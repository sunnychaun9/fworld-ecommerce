/**
 * QueryProvider — placeholder (no implementation).
 *
 * Intended to wrap the application in a TanStack Query `QueryClientProvider`
 * to own server state: caching, background refetching, deduplication, and
 * retry logic (docs/009_CODING_STANDARDS.md → "Server State: TanStack Query").
 *
 * Implementation is deferred to a later phase. Repository foundation only.
 */
export {};
