/**
 * AnalyticsProvider — placeholder (no implementation).
 *
 * Intended to initialize and expose analytics context (Google Analytics 4 /
 * Microsoft Clarity) per docs/013_ANALYTICS.md, gated on consent and the
 * NEXT_PUBLIC_* environment variables.
 *
 * Implementation is deferred to a later phase. Repository foundation only.
 */
export {};
