/**
 * ThemeProvider — placeholder (no implementation).
 *
 * Intended to provide design-token / color-scheme context (light/dark) for the
 * UI, backed by the tokens defined in app/globals.css and
 * docs/006_DESIGN_SYSTEM.md.
 *
 * Implementation is deferred to a later phase. Repository foundation only.
 */
export {};
