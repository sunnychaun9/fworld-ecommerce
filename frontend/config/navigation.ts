/**
 * navigation.ts — placeholder (no implementation).
 *
 * Intended home for the typed navigation model: header/footer menus, category
 * trees, and route maps consumed by layout components.
 *
 * Implementation is deferred to a later phase. Repository foundation only.
 */
export {};
