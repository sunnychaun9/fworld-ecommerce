/**
 * app.ts — placeholder (no implementation).
 *
 * Intended home for typed, app-wide runtime configuration (app name, public
 * URLs, locale/currency defaults, runtime flags) sourced from NEXT_PUBLIC_*
 * environment variables. See the root .env.example.
 *
 * Implementation is deferred to a later phase. Repository foundation only.
 */
export {};
