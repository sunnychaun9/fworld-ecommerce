/**
 * seo.ts — placeholder (no implementation).
 *
 * Intended home for default SEO configuration: site metadata, Open Graph /
 * Twitter card defaults, canonical/base URL, and JSON-LD organization data,
 * per docs/008_SEO.md (SEO-as-infrastructure).
 *
 * Implementation is deferred to a later phase. Repository foundation only.
 */
export {};
