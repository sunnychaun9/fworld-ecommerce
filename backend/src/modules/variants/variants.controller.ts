import { Controller } from '@nestjs/common';

/**
 * Variants endpoints (Product Catalog foundation — scaffold).
 * Route handlers are added when CRUD is implemented.
 */
@Controller('variants')
export class VariantsController {}
