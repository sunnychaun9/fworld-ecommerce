import { Injectable } from '@nestjs/common';

/**
 * Variants data-access layer (Product Catalog foundation — scaffold).
 * The PrismaService dependency and query methods are added when CRUD lands.
 */
@Injectable()
export class VariantsRepository {}
