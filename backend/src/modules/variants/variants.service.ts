import { Injectable } from '@nestjs/common';

/**
 * Variants domain service (Product Catalog foundation — scaffold).
 * The VariantsRepository dependency and business logic are added in a later slice.
 */
@Injectable()
export class VariantsService {}
