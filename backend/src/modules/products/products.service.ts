import { Injectable } from '@nestjs/common';

/**
 * Products domain service (Product Catalog foundation — scaffold).
 * The ProductsRepository dependency and business logic are added in a later slice.
 */
@Injectable()
export class ProductsService {}
