import { Controller } from '@nestjs/common';

/**
 * Products endpoints (Product Catalog foundation — scaffold).
 * Route handlers are added when CRUD is implemented.
 */
@Controller('products')
export class ProductsController {}
