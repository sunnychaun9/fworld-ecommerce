import { Controller } from '@nestjs/common';

/**
 * Inventory endpoints (Product Catalog foundation — scaffold).
 * Route handlers are added when CRUD is implemented.
 */
@Controller('inventory')
export class InventoryController {}
