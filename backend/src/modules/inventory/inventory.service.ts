import { Injectable } from '@nestjs/common';

/**
 * Inventory domain service (Product Catalog foundation — scaffold).
 * The InventoryRepository dependency and business logic are added in a later slice.
 */
@Injectable()
export class InventoryService {}
