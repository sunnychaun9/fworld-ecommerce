import { Injectable } from '@nestjs/common';

/**
 * Brands domain service (Product Catalog foundation — scaffold).
 * The BrandsRepository dependency and business logic are added in a later slice.
 */
@Injectable()
export class BrandsService {}
