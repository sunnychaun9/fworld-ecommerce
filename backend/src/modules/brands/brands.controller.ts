import { Controller } from '@nestjs/common';

/**
 * Brands endpoints (Product Catalog foundation — scaffold).
 * Route handlers are added when CRUD is implemented.
 */
@Controller('brands')
export class BrandsController {}
